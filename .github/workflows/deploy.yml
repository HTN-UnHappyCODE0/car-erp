name: Deploy Car ERP to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH Key & Cloudflare Zero Trust Tunnel
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb

          cat << 'EOF' > ~/.ssh/config
          Host ssh.namhoanglegal.com
              HostName ssh.namhoanglegal.com
              User ${{ secrets.SERVER_USER }}
              Port 22
              IdentityFile ~/.ssh/id_rsa
              ProxyCommand cloudflared access ssh --hostname %h --service-token-id ${{ secrets.CF_ACCESS_CLIENT_ID }} --service-token-secret ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
              StrictHostKeyChecking no
              UserKnownHostsFile=/dev/null
          EOF
          chmod 600 ~/.ssh/config

      - name: Execute Deployment on Remote Server via Tunnel
        run: |
          ssh -T -F ~/.ssh/config ssh.namhoanglegal.com << 'EOF'
            cd ~/car-erp
            git pull origin main
            docker compose up --build -d
            docker image prune -f
          EOF

      # KHỐI MỚI: GỬI THÔNG BÁO TELEGRAM
      - name: Send Telegram Notification
        uses: appleboy/telegram-action@master
        if: always() # Bắt buộc chạy khối này dù quá trình deploy thành công hay thất bại
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🚀 Cập nhật hệ thống Car ERP!
            Trạng thái: ${{ job.status }}
            Nhánh: ${{ github.ref_name }}
            Người push code: ${{ github.actor }}
            Message: ${{ github.event.commits[0].message }}
